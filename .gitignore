# Laravel environment files
.env

# Composer dependencies
/vendor/

# Node dependencies
/node_modules/

# Laravel storage logs and caches (se incluyen los .gitignore para que no se borren las carpetas)
storage/*.log
storage/framework/cache/*
storage/framework/sessions/*
storage/framework/views/*

# Backup and temp files
*.bak
*.backup
*.tmp
*.temp
*~

# OS generated files
.DS_Store
Thumbs.db

# IDEs and editors
.vscode/
.idea/

# Docker logs and overrides
*.log
docker-compose.override.yml

# Docker persistent volume data for MySQL (no subir datos de base de datos)
mysql_data/

# SSH keys and private keys
*.pem
*.ppk
labsuser.pem

# Entrypoint script logs or cache if any
entrypoint.sh~

# Laravel specific cache and config files (si quieres limpiar caché en el contenedor es mejor, no subirlos)
bootstrap/cache/*.php

# Otros archivos generados o temporales que no quieras subir

